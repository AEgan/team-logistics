<!DOCTYPE html>
<html>
	<head>
		<title>New Ride</title>
		<script
      src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js">
    </script>
    <script type="text/javascript" src="../../../javascripts/application.js"></script>
    <link rel="stylesheet" type="text/css" href="../../../stylesheets/bootstrap.css">
    <link rel="stylesheet" type="text/css" href="../../../stylesheets/application.css">
	</head>
	<body>
		<% if(current_user) { %>
		<% include loggedInNavigation.ejs %>
		<% } else { %>
		<% include navigation.ejs %>
		<% } %>
		<div id="mainContent">
			<% if(warning) { %>
			<div class="warning">
		      <p><%= warning %></p>
		  </div>
		  <% } %>
			<h1><%= 'New Ride for ' + teamName + "'s " + eventName %></h1>
			<form id="newRideForm" action=<%='/teams/'+teamName+'/'+eventName+'/newRide'%> method='post'>
				Departure Time <input type="datetime-local" name="time"><br />
				Number of spots <input type="number" name="spots"><br />
				<input type="submit" value="Create">
			</form>
		</div>
	</body>
</html>